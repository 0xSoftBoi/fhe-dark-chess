interface:
  display_name: "Fhe Dark Chess"
  short_description: "Repo-specific patterns and workflows for fhe-dark-chess"
  default_prompt: "Use the fhe-dark-chess repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true